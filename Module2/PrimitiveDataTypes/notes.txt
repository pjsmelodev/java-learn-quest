TYPES OF CONSTRUCTS:
    OUTERMOST BLOCKS
        Classes,
        Interfaces,
        Annotations,
        Enums
    OTHER TYPES
        Arrays,
        Primitives

O compilador assume que as primitives são ints
Long -> long myLong = 23456L;

O compilador assume que as primivas são doubles
Float -> float myFloat = 234.78F

Existe NaN e infinity em Java

SINTAXE:
int x;
boolean isStarted;

int x, y, z;

int x = 3;

Se as variáveis não tiverem atribuição o Java dá-lhes um valor ("neutro")

ACTIVITY:
    Which of the following are ptimitives? (Select all that apply)
        boolean flag = true;
        byte b = 100;
        int a, b, c;
        short temperature;
        boolean isAcceptable;