TYPES OF CONSTRUCTS:
    OUTERMOST BLOCKS
        Classes,
        Interfaces,
        Annotations,
        Enums
    OTHER TYPES
        Arrays,
        Primitives

O compilador assume que as primitives são ints
Long -> long myLong = 23456L;

O compilador assume que as primivas são doubles
Float -> float myFloat = 234.78F

Existe NaN e infinity em Java

SINTAXE:
int x;
boolean isStarted;

int x, y, z;

int x = 3;

Se as variáveis não tiverem atribuição o Java dá-lhes um valor ("neutro")

ACTIVITY:
    Which of the following are ptimitives? (Select all that apply)
        boolean flag = true;
        byte b = 100;
        int a, b, c;
        short temperature;
        boolean isAcceptable;

KNOWLEDGE CHECK:
1. What is the default value of a boolean variable in Java if not explicitly initialized?
    false

2. How can you initialize a multidimensional array in Java?
    int [][] arr = {{1,2},{3,4}};

3. In Java, which primitive data type is used to represent true or false values?
    boolean